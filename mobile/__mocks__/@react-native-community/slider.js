const React = require('react');
const { View } = require('react-native');

const Slider = (props) => React.createElement(View, { testID: props.testID });

module.exports = { __esModule: true, default: Slider };
